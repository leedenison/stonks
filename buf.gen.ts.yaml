# protobuf-es types and service descriptors for the client. Runs from the
# repository root with --include-imports, so the dependency descriptors the
# generated files reference are emitted beside them.
version: v2
clean: true
plugins:
  - local: client/node_modules/.bin/protoc-gen-es
    out: client/gen
    opt: target=ts
